{
  "name": "multimodal-survey-collector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:init:local": "wrangler d1 execute multimodal-survey-results --local --file=./schema.sql",
    "d1:init:remote": "wrangler d1 execute multimodal-survey-results --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.52.0"
  }
}
